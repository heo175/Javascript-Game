객체 = 함수 + 배열 + 함수와 배열이 아닌 객체

객체는 값을 그룹화할 때 쓰면 좋다.

var tc = {
    name: 'taek',
    height: 178,
}

객체 = {
  속성(프로퍼티): 값,
  속성(메서드): 함수
}

속성 접근
tc.name
tc['name']


-------------------------------------------

배열 : 값 그룹화는 하지만 속성 이름을 따로 주고 싶지 않을 때 사용한다.

Array.isArray() : 배열이면 true

